name: Deploy to dev

on:
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  deploy:
    uses: sevria/actions/.github/workflows/deploy-cloudflare-workers.yaml@main
    secrets: inherit
    with:
      environment: prod
